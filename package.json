{
    "name": "terrain",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "scripts": {
        "build": "tsc --noEmit && bun run build.ts"
    },
    "dependencies": {
        "@minecraft/vanilla-data": "^1.26.2"
    }
}
